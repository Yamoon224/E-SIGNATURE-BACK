package com.signature.application.usecases;

public class LoginUseCase {
    public String execute(String email, String password) {
        // logique métier (recherche user, vérification, JWT)
        return "fake-jwt-token";
    }
}